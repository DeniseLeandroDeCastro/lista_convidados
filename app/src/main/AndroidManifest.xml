<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.convidados">

    <application
        android:allowBackup="true"
        android:icon="@drawable/guest_list"
        android:label="@string/app_name"
        android:roundIcon="@drawable/guest_list"
        android:supportsRtl="true"
        android:theme="@style/Theme.Convidados">
        <activity
            android:name="com.example.convidados.view.activity.GuestFormActivity"
            android:exported="false"
            android:theme="@style/Theme.Design.NoActionBar"/>
        <activity
            android:name="com.example.convidados.view.activity.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Convidados.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>